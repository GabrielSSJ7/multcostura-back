version: "3"

services:
  app:
    build: .
    command: npm start
    ports:
      - "4000:4000"
    volumes:
      - .:/usr/app
    links:
      - mongo
  mongo:
    container_name: mongo
    image: mongo
    ports:
      - "27017:27017"
    logging:
      driver: none
